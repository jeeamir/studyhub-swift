# StudyHub

Academic management app for Narxoz University students.

## Setup
1. Clone the repo
2. Create `Config.xcconfig` based on `Config.xcconfig.example`
3. Add your Supabase credentials
4. Run in Xcode

## Tech Stack
- Swift + SwiftUI + UIKit
- Supabase (Auth + Database)
- Core Data (offline cache)